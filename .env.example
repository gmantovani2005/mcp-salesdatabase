# Conexão com o PostgreSQL (banco já existente e populado)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=salesdb
POSTGRES_USER=salesuser
POSTGRES_PASSWORD=changeme
POSTGRES_POOL_MAX=5

# Transporte do MCP: "stdio" para desenvolvimento, "sse" em container
MCP_TRANSPORT=stdio
MCP_SSE_HOST=0.0.0.0
MCP_SSE_PORT=8000
